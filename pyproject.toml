[project]
name = "weatherml"
version = "0.1.0"
description = "weatherml - automated paper curation from arXiv"
requires-python = ">=3.11"
dependencies = [
    "zensical>=0.0.65",
    "pyyaml",
    "arxiv",
]
